{
  "name": "@grantmcd/skills",
  "version": "1.0.0",
  "description": "A collection of specialized, high-quality, and objectively evaluated skills for AI agents.",
  "devDependencies": {
    "prettier": "^3.8.1"
  },
  "scripts": {
    "lint": "ruff check .",
    "lint:fix": "ruff check --fix .",
    "format": "ruff format . && prettier --write \"**/ui/*.html\"",
    "format:check": "ruff format --check . && prettier --check \"**/ui/*.html\"",
    "test": "npm run lint && npm run format:check"
  }
}
